#!/usr/bin/env ruby
require "rubygems"
require "bundler/setup"

# Removed --ensure-latest flag to prevent CI failures when not using the absolute latest version
# ARGV.unshift("--ensure-latest")

load Gem.bin_path("brakeman", "brakeman")
